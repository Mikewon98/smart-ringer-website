services:
  watchtower:
    image: nickfedor/watchtower
    restart: unless-stopped
    command:
      - "--label-enable"
      - "--rolling-restart"
      - "--include-restarting"
      - "--stop-timeout"
      - "60s"
      - "--scope=smart-ringer-website"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    labels:
      - "com.centurylinklabs.watchtower.enable=false"
    environment:
      - WATCHTOWER_POLL_INTERVAL=300
      - REPO_USER=mikewon98
      - REPO_PASS=${GITHUB_TOKEN}
      - WATCHTOWER_NOTIFICATIONS=shoutrrr
      - WATCHTOWER_NOTIFICATION_URL=${DISCORD_WEBHOOK_URL}
  smart-ringer-website:
    image: ghcr.io/mikewon98/smart-ringer-website:stable
    restart: unless-stopped
    networks:
      - portfolio_default
    labels:
      - "traefik.enable=true"

      # Main HTTPS router
      - "traefik.http.routers.smart-ringer-website.rule=Host(`smartringer.michaelyze.com`)"
      - "traefik.http.routers.smart-ringer-website.entrypoints=websecure"
      - "traefik.http.routers.smart-ringer-website.tls.certresolver=myresolver"

      # HTTP redirect router
      - "traefik.http.routers.smart-ringer-website-http.rule=Host(`smartringer.michaelyze.com`)"
      - "traefik.http.routers.smart-ringer-website-http.middlewares=redirect-to-https-en"
      - "traefik.http.routers.smart-ringer-website-http.entrypoints=web"

      # Service configuration
      - "traefik.http.services.smart-ringer-website.loadbalancer.server.port=3000"

      # Middlewares
      - "traefik.http.middlewares.redirect-to-https-en.redirectscheme.permanent=true"
      - "traefik.http.middlewares.redirect-to-https-en.redirectscheme.scheme=https"

      # Rate limiting (protection against bots)
      - "traefik.http.middlewares.rate-limit.ratelimit.average=100"
      - "traefik.http.middlewares.rate-limit.ratelimit.burst=50"
      - "traefik.http.middlewares.rate-limit.ratelimit.period=1s"

      # Watchtower
      - "com.centurylinklabs.watchtower.enable=true"
      - "com.centurylinklabs.watchtower.scope=smart-ringer-website"

    env_file:
      - .env

networks:
  portfolio_default:
    external: true
